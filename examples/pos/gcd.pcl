program gcd;
      var a, b, t : integer;
  begin
      b := 12;
      a := 16;
      while b >= 1 do
      begin
          t := b;
          b := a mod b;
          a := t;
      end;
      writeInteger(a);
  end.
